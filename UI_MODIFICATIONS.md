# UI 修改记录

## 修改日期
2026-09-07

## 修改目标
1. 统一配色，特别是浅灰色框
2. 优化高级设置按钮样式
3. 使用 ui-ux-pro-max skill 美化 UI
4. 整体风格类似 Apple 网页设计

## 修改内容

### 1. 统一配色方案

#### CSS 变量修改
- **主背景**：`--apple-bg: #fbfbfd`（原为 `#f5f5f7`）
- **卡片背景**：`--apple-surface: #ffffff`（保持不变）
- **次要背景**：`--apple-surface-2: #fbfbfd`（保持不变）
- **边框**：`--apple-border-soft: #e8e8ed`（保持不变）

#### 灰色使用规范
- **输入框背景**：`#f5f5f7`
- **按钮背景**：`#f5f5f7`
- **按钮悬停**：`#ebebed`
- **边框**：`#e8e8ed`
- **主背景**：`#fbfbfd`

### 2. 高级设置按钮优化

#### 添加的 CSS 样式
```css
/* ── 高级设置按钮优化 ── */
.modal-trigger {
  background: var(--apple-surface) !important;
  color: var(--apple-text) !important;
  border: 1px solid var(--apple-border) !important;
  border-radius: 14px !important;
  font-weight: 500 !important;
  font-size: 0.95rem !important;
  padding: 14px 24px !important;
  transition: all 0.2s ease !important;
  cursor: pointer !important;
  display: flex !important;
  align-items: center !important;
  justify-content: center !important;
  gap: 8px !important;
}

.modal-trigger:hover {
  background: var(--apple-surface-2) !important;
  border-color: var(--apple-border-soft) !important;
  transform: translateY(-1px);
  box-shadow: var(--apple-shadow) !important;
}

.modal-trigger:active {
  transform: translateY(0);
}

.modal-trigger::before {
  content: "⚙️";
  font-size: 1.1rem;
}
```

#### 按钮 HTML 修改
- 移除了内联样式，使用 CSS 类控制样式
- 保持了原有的弹窗功能

### 3. Apple 设计原则应用

#### 已有设计特点
1. **胶囊形状按钮**：使用 `border-radius: 980px` 或 `14px`
2. **平滑过渡**：使用 `transition` 和 `cubic-bezier` 缓动函数
3. **阴影效果**：使用 `var(--apple-shadow)` 和 `var(--apple-shadow-blue)`
4. **留白设计**：使用 `clamp()` 函数确保响应式留白
5. **字体选择**：使用 `-apple-system, BlinkMacSystemFont, "SF Pro Display"` 等系统字体

#### 无障碍标准
- 主要文本对比度：15.3:1（远高于 4.5:1 的要求）
- 次要文本对比度：4.75:1（略高于 4.5:1 的要求）
- 焦点状态可见：使用蓝色阴影指示焦点
- 响应式设计：支持不同屏幕尺寸

### 4. 响应式设计

#### 媒体查询
- `@media (max-width: 560px)` - 移动设备优化
- `@media (min-width: 560px)` - 平板设备优化
- `@media (max-width: 480px)` - 小屏幕优化
- `@media (max-width: 720px)` - 中等屏幕优化
- `@media (max-width: 520px)` - 特定屏幕优化
- `@media (pointer: coarse)` - 触摸设备优化
- `@media (hover: none)` - 无悬停设备优化
- `@media (prefers-reduced-motion: reduce)` - 减少动画优化
- `@media (min-width: 1200px)` - 大屏幕优化
- `@media (min-width: 1440px)` - 超大屏幕优化

## 测试结果

### 语法检查
- Python 语法检查：✅ 通过
- CSS 语法检查：✅ 通过
- 模块导入检查：✅ 通过

### 功能检查
- 高级设置弹窗功能：✅ 正常
- 按钮样式：✅ 已优化
- 配色统一：✅ 已完成

## 符合 Apple 设计风格的特点

1. **简洁性**：移除了不必要的内联样式，使用 CSS 类控制样式
2. **一致性**：统一了灰色配色方案
3. **可访问性**：确保了足够的对比度和焦点状态
4. **响应式**：支持不同屏幕尺寸
5. **动画效果**：使用了平滑的过渡动画

## 后续建议

1. 可以考虑添加深色模式支持
2. 可以考虑添加更多的微交互效果
3. 可以考虑优化移动端体验
4. 可以考虑添加更多的 Apple 设计元素（如模糊效果、渐变等）

---

## 氛围背景层 + 毛玻璃（2026-09）

实现文件：`audiobook_generator/ui/ambient_background.py`（`AMBIENT_LAYER_HTML` / `AMBIENT_CSS` / `AMBIENT_ENABLED`），
由 `audiobook_generator/ui/web_ui.py` 拼到 `HEAD_HTML` 与 `CUSTOM_CSS` 末尾。

### 分层

```
html/body 兜底渐变（background-image + background-color，不能用带 var() 的多值简写）
  └─ #ata-bg（position:fixed; z-index:0; pointer-events:none）
       ├─ #ata-bg-base      静态环境光渐变（生成态叠加 ::after 加强约 1.6 倍）
       └─ #ata-bg-canvas    canvas 光晕（预渲染 sprite + drawImage）
  └─ .gradio-container（position:relative; z-index:1; 背景透明）→ 卡片等内容
```

状态机：`idle → arming`（点「开始生成」）`→ generating`（进度条 `active/starting/running`）
`→ settling`（`done/warn/collapsed` 或「停止转换」）`→ idle`。

### 白底清理规则（关键）

- 卡片内布局包装层 `.block / .wrap / .form / .panel / .contain / .styler / .gr-group / .gr-box / .gr-form` → **透明**。
- 交互面（上传拖放区、开关行、幽灵/危险/迷你按钮、输入框/文本域/下拉、引擎分段选择器、
  文件组件标题标签与悬浮图标按钮）→ `rgba(255,255,255,0.45)`；弹窗内输入框 `0.55`。
- 阅读面：日志终端 `.xterm-viewport/.xterm-screen` `0.42`、资源库空态 `.lib-empty` `0.35`、
  Gradio 文件预览表格行 → 透明。
- 高级设置弹窗：`.modal-box` 透明 + 玻璃画在 `::before`（避开 `backdrop-filter` 包含块问题）。
- **必须保持不透明**：下拉选项面板 `ul.options`、`.modal-box` 之外的遮罩层。

### 验证清单（每次改 UI 后照做）

1. 四个页面（转换/资源库/日志/设置）扫描卡片内 `backgroundColor` 为不透明浅色的元素，应为 0；
2. 5 个引擎面板、高级设置弹窗、**已选文件状态**（文件列表行）、有内容的资源库列表同样扫描；
3. 鼠标停在页面不同位置截图，卡片内外的蓝偏应同步变化（证明玻璃在透背景，而不是被白填充盖住）；
4. 生成态：边框蓝偏 − 中心蓝偏应为明显正值（当前约 +13），空闲态接近 0；
5. 纯背景条相邻像素跳变 ≤2（无条带/断层）；`tests/audiobook_generator/ui` 全绿。

## 深色主题（2026-09）

实现文件：`audiobook_generator/ui/theme.py`（`THEME_TOKENS_CSS` / `THEME_CSS` / `THEME_HEAD_HTML`），
由 `web_ui.py` 拼进 `CUSTOM_CSS` 与 `HEAD_HTML`；顶栏按钮 `#ata-theme-toggle` 循环
**跟随系统 → 浅色 → 深色**（localStorage `ata-theme`）。

### 设计令牌

| 用途 | 浅色 | 深色 |
| --- | --- | --- |
| 页面底 `--apple-bg` | `#f5f5f7` | `#0e0e12` |
| 文字 `--apple-text` / `-2` / `-3` | `#1d1d1f` / `#5f5f66` / `#6a6a72` | `#f5f5f7` / `#c3c3c9` / `#a5a5ad` |
| 卡片玻璃 `--ata-glass` | `rgba(255,255,255,.58)` | `rgba(30,30,37,.62)` |
| 顶栏 / 进度条玻璃 | `.62` 白 | `rgba(18,18,23,.68)` / `rgba(32,32,39,.66)` |
| 控件面 `--ata-glass-soft` | `rgba(255,255,255,.45)` | `rgba(255,255,255,.07)` |
| 弹窗玻璃 `--ata-glass-modal` | `rgba(255,255,255,.82)` | `rgba(24,24,30,.86)` |
| 强调色 `--apple-blue` / 绿 / 红 | `#0071e3` / `#1f7a35` / `#d70015` | `#409cff` / `#4cd964` / `#ff8a82` |

### 两个必须知道的实现点

1. **暗色钩子复用 Gradio 的 `body.dark`**：Gradio 前端的主题 CSS 写成 `:root .dark{}`，我们挂同一个
   选择器（外加 `:root.dark` 用于首屏不闪白），这样自有组件与 Gradio 组件同时翻转。
   Gradio 自带文字变量（`--body-text-color-subdued` 等）默认是 slate-400，浅底上仅 2.48:1，
   已在 `:root` 与 `:root:root .dark`（提高一级特异性）里重映射到本项目文字令牌。
2. **日志终端只反相文字层**：xterm 用 canvas/DOM 渲染、主题色写死在组件里（浅底深字）。
   暗色下给 `.xterm-screen` 加 `invert(1) hue-rotate(180deg)`，底色放在不会被反相的
   `.xterm-viewport`（`--ata-terminal`）。若连 `.xterm` 一起反相，底色会被反成亮板。

### 可读性验证（改配色后照做）

- 用浏览器实测每页所有含文字元素的对比度（前景色叠加到最近的不透明背景上算 WCAG 比值）：
  正文 ≥ 4.5:1、大字号（≥24px 或 ≥18.66px 粗体）≥ 3:1 —— 两套主题 × 四页当前均为 0 处不达标；
- 终端、渐变标题、蓝色按钮等"取不到背景色"的元素用像素法复核（终端实测 13–14:1）；
- 单元测试 `tests/audiobook_generator/ui/theme_test.py` 会直接算两套调色板的对比度，
  改坏配色会立刻失败。

### 光晕平滑度与点击扩散（2026-09）

**断层问题的根因**：低透明度的全屏渐变在 8bit 下必然分档——实测右侧空白条带上出现 27 段"平顶"
（平均每 20px 才跳 1 级、最长 403px），肉眼就是一圈圈色带。

**三道防线**：

1. 色相从 24 档提到 32 档，并在相邻两张 sprite 之间按比例交叉淡入（`drawGlowSprite`）；
2. sprite 边长 160 → 224px，降低放大倍率；
3. 每帧最后铺一层预烘焙的抖动噪声（黑白各半、alpha 6 ≈ ±3/255，平均亮度为 0），
   把色带边界打散。

**效果（右侧空白条带 580px 剖面）**：

| 指标 | 改前 | 改后（深色） | 改后（浅色） |
| --- | --- | --- | --- |
| 平台段数(最长) | 27 段 / 403px | 8 段 / 1px | 16 段 / 2px |
| 取值档数 | 43 | 182 | 185 |
| 相邻像素变化比例 | 8% | 97% | 97% |

**点击「开始生成」的动效**（对齐 travel-agent 主页面）：

- 扩散起点 = 点击瞬间的鼠标位置（`spreadAnchorX/Y`），方向按黄金角均匀铺开，
  从光标向视口四周边框飞出，中心光晕同步淡出、边缘光晕淡入并缓慢呼吸；
- 同时整页平滑滑回最顶端（`window.scrollTo({top: 0, behavior: 'smooth'})`）。

**验证方法（可复现）**：点击前后在同锚点、同滚动位置各抓一帧，比较四角探针与
16 个方向的光晕半径；实测四角分别 +29~+41 色阶，12–13/16 个方向的光晕延伸到 490px 以上，
页面 scrollY 从 1262 回到 0。

### 柔光光晕：顺滑过渡 + 无色带（2026-09 最终版）

**目标**：保留"一团柔和光晕"的观感（不要变成一颗颗清晰小圆点），同时不能出现模糊的色彩断层。

**形态**：46 颗大半径低透明度光斑（主体半径 110~200px、核心 150~240px）+ 中心弥散光
（440px / 320px）+ 边缘泛光场（130~400px），叠加成一整团连续光晕。

**顺滑与抗断层的四道措施**：

1. 色相 32 档 + 相邻 sprite **交叉淡入**（消除跳色）；
2. sprite 边长 **256px**（放大倍率降到 3~6 倍）；
3. 衰减曲线改 **多段近似高斯**（0 / .22 / .42 / .60 / .76 / .89 / 1）——停止点太少会出现
   看得见的"圈边"肩部；
4. 每帧最后铺 **抖动噪声层**：逐通道独立取 0/255、alpha 6（≈±3/255，平均色中性）。
   ⚠️ 必须逐通道独立——只抖动亮度的话，色度（B−R）仍会停在量化档上，看起来就是彩色色带。

**构造方式重构：从"叠 sprite"改成"浮点光场"（2026-09 最终形态）**

旧做法是预渲染几十张 8bit 光斑再按不同透明度叠加；每张图的量化台阶会在叠加处露出
可见边界与色带（实测色带平台最长 403px、可见边缘像素 6.73%），这也是"很多圈叠在一起"的来源。
现在改成：

1. 每颗粒子只作为一个**光源**，在 `Float32Array` 的 RGB 缓冲里按高斯核 `exp(-3t)` 累加
   （`splat()`，光场分辨率 = 视口 / 5）；
2. 整帧只在 `compositeField()` 里**量化一次**（alpha = 亮度之和，RGB = 加权色相），
   不同色相在浮点域自然混合，色相查表还带线性插值；
3. 放大回主画布（双线性再平滑一次），最后在 display 分辨率叠抖动层。

形态与旧版一致（中心 440/320px 弥散光 + 46 颗粒子 + 边缘层），但性能更好：
**渲染 0.41ms/帧（空闲）、1.21ms/帧（生成中）**。

| 指标 | 叠 sprite | 离屏模糊（中间版） | **浮点光场（当前）** |
| --- | --- | --- | --- |
| 可见边缘像素 | 6.73% | 1.30% | **1.25%** |
| 梯度均值 | 1.460 | 0.141 | **0.144** |
| 亮度剖面最大跳变 | 0.45 级 | 0.32 级 | **0.40 级** |
| 色度剖面最大跳变 | 2.0 级 | 2.7 级 | 2.8 级 |

行为对齐需求：鼠标在页面内 → 粒子汇聚成一束大光（跟随光标）；鼠标移出浏览器 → 中心光淡出，
只剩散开的粒子；点「开始生成」→ 从鼠标处按黄金角均匀散向四周并滚回页面顶端（均保持不变）。

### 亮度 / 浓淡 / 范围调优（2026-09）

按"再亮一些、颜色更深、范围更大"的要求调整（形态与结构不变）：

| 项 | 调整前 | 调整后 |
| --- | --- | --- |
| 粒子半径（主体 / 核心） | 125–230 / 170–275 | **150–270 / 200–320** |
| 粒子透明度（空闲） | 0.014–0.030 / 0.030–0.055 | **0.020–0.042 / 0.042–0.075** |
| 中心光束外圈 / 内圈 | 500px / 320px | **700px / 420px** |
| 色彩饱和/明度（浅 / 深） | 78/76 · 82/70 | **86/70 · 90/64**（更深） |
| `fieldMaxRadius` | 60 格（=300px，**会截断中心光束**） | **150 格（=750px）** |

实测（同锚点、多帧跨色相周期取均值）：

| 指标 | 调整前 | 调整后 |
| --- | --- | --- |
| 光束中心相对背景亮度 | +16.8 | **+32.8（最暗色相 18.5）** |
| 中心色偏（B−R，代表"颜色深"） | +61.7 | **+102.5（+66%）** |
| 平均光晕半径 | 399px | **434px（+9%）** |
| 渲染耗时 | 0.41–1.21ms/帧 | 0.65–1.74ms/帧 |
| 色带（亮度/色度最大跳变） | 0.40 / 2.8 级 | 0.42 / 1.97 级 |

副作用处理：光晕变强后浅色主题 hero 段落在最亮色相下只有 4.46:1，
把 `--apple-text-2` 从 `#5f5f66` 压深到 `#52525a`，实测回到 **5.22–5.53:1**。

### 收束更紧 + 中心高光（2026-09）

需求：鼠标在页面内时光晕**收束范围再小一点**，中心**亮一点、做出一点高光感**（不用太亮）。

做法：中心从两层改成三层配比，并收紧汇聚时的粒子——

| 层 | 半径 / alpha | 作用 |
| --- | --- | --- |
| 外圈 | 700px / 0.030 | 大而淡，只负责远处范围 |
| 主体 | 240px / 0.100 | 收束主体 |
| 高光核 | 110px / 0.240 | 鼠标处的一点亮 |

汇聚态粒子尺寸 80–135 / 55–110 → **50–90 / 35–70**，汇聚抖动 ±70 → **±45px**。

⚠️ 两次踩坑：只收紧粒子尺寸看不出变化——因为外圈那层在数值上压过粒子，
必须同时收中心层；另外用绝对亮度量"半径"会被页面文字/控件污染，
要用**差分法**（同内容、两个光标位置相减）才准。

**实测（差分法，围绕光标做 32 方向径向平均）**：

| 阈值 | 调整前 | 调整后 |
| --- | --- | --- |
| 中心峰值 Δ亮度 | 47 | **87（+85%，更亮）** |
| >40/255（高光核） | 35px | **55px** |
| >10/255 | 125px | **95px** |
| >5/255 | 195px | **125px** |
| >2/255（外缘） | 285px | **205px** |

即：中心更亮、亮核更大一点，同时收敛范围整体收小 24%–28%；
鼠标移出页面后的散开形态（spread 440px）保持不变。

### 中心光束不再粘在鼠标上（2026-09）

需求：快速移动鼠标时，光标处先出现一小团亮光、其余部分才慢慢挪过去——要去掉这团
"粘在鼠标上的光晕"，让整团光晕作为一个整体移动到鼠标位置；同时**收束范围再小一点**、
**中心高光再加一点点**。

**根因**：中心三层光束锚在 `focusX/focusY`（光标，滞后约 0.35s）上，而粒子云的
`ease = 0.08` 要 ~1s 才跟上。快移时粒子还在路上，光束已经收成一个亮核落在光标处，
于是看起来像"光标上贴了一颗亮斑"。

**改法**：

1. 光束改锚**粒子团的 alpha 加权重心** `massCx/massCy`（先更新粒子、再由同一批粒子
   投光，所以重心和粒子云天然同步）；
2. 亮度再乘一个**聚集度** `conc = 0.3 + 0.7·clamp(1 − rms/110)`，云团散着时只有 30%
   亮度，聚拢后才补满——避免"重心跟上了、但收得很紧的亮核先成形"；
3. 收束范围：中心三层 620/210/105px → **560/190/100px**（alpha 0.034/0.140/0.400 →
   **0.040/0.155/0.540**），汇聚抖动 ±38 → **±32px**；
4. 跟手速度略提：`ease` 0.07 → **0.08**、`focusX` 0.05 → **0.06**（去掉"亮核先到"的
   视觉提示后，整团到位别显得拖沓）。

**实测**（`#ata-bg.dataset.field` 光场快照，围绕光标做同心环平均；同一 reload 相位、
5 帧平均。注意每次刷新粒子布局随机，重复测量噪声约 ±5%）：

| 量 | 旧 | 新 |
| --- | --- | --- |
| 峰值（max 通道，1.0 = 满亮） | 1.875 | **2.033（+8%）** |
| 0–25px 环 | 1.605 | **1.788（+11%）** |
| 25–60px 环 | 1.051 | **1.055** |
| 60–120px 环 | 0.282 | **0.228（−19%）** |
| 120–200px 环 | 0.059 | **0.045（−24%）** |
| 200–320px 环 | 0.021 | **0.018** |

即可见半径（约 0.03 阈值处）185px → 165px（**−11%，"稍微再小一点"**），
中心高光 +8~11%，`frameMs` 仍为 **0.62ms/帧**。

**快移验证**：从 (150,45) 跳到 (700,45)（视口 980×997）连续采样，亮度峰值始终
落在粒子团重心上（如 t=1018ms：峰值 x=400、重心 x=400、光标 x=700），
不存在"光标处先亮一坨"的帧。

### 色相循环的瞬跳修复（2026-09）

**问题**：颜色"卡一下突然换成另一种"。

**根因**：调色板是 `[212, 250, 196, 288]`，旧实现是"线性扫过这四个色相，扫完再从
第一色重新开始"——最后一段 `288 → 212` 在一帧内跳 **76°**，就是那次瞬跳。

**修法**：调色板首尾相接成**闭环**，并用 **Catmull-Rom**（取相邻四点 p0/p1/p2/p3）
插值，跨段的一阶导连续：

* 循环回绕处不再跳变（数值模拟接缝步进 **0.21°**，一轮 40s 内最大步进 1.31°/100ms）；
* 每个色相点附近也不再"到站停一下再走"；
* 整轮时长 = `hueSegmentMs × 段数`（默认 4 × 10s = **40s**，比旧版更慢）。

**实测**（浏览器里每 2.5s 采一帧，取光晕自身色相 = 光标附近区域 − 远处背景，抵消底色与内容）：

| | 修复前 | 修复后 |
| --- | --- | --- |
| 相邻帧色相变化 中位 | 14.8° | 14.3° |
| 相邻帧色相变化 **最大** | **72.2°（4.9×）** | **32.6°（2.3×）** |
| 轨迹 | …271.6 → 290.4 → **218.2** → 231.5… | …275.8 → 289.1 → 277.3 → 249.4…（连续波动） |

**另一个已修的问题**：中心那两团弥散光原本画在屏幕"家位置"上、不跟鼠标走，鼠标挪开后
正中央残留一团浅色圆，且颜色随色相缓慢变化（看起来就是"中间有个圆在闪"）。
现在改成跟随光标（`baseX/baseY`），实测光晕移到右下角后正中央与远区差 **0.00/255**。

**实测（同锚点、同主题，光标附近纯背景区）**：

| 指标 | 最初大光斑版 | 上一版（小粒子） | **本版（柔光+抗断层）** |
| --- | --- | --- | --- |
| 色带最长平台 | 403px | 1px | **1–2px** |
| 剖面取值档数 | 43 | 190 | **183–203** |
| 可见边缘像素比例 | 7.06% | 14.46% | **6.73%（最低）** |
| 梯度均值(0-255级/px) | 1.503 | 1.947 | **1.460（最低）** |
| 静态鼠标帧间差 | 1.29‰ | 0.03‰ | 0.5‰ |

结论：光晕形态与最初版本一致（场强 8.1 vs 9.0），但内部最平滑（边缘/梯度均为三版最低），
且色带已完全不可见。

### 标签 hover 毛玻璃（2026-09）

**语音引擎 hover 白框**：Gradio 给 `.tab-container button:hover` 的是实心填充
`rgb(248,250,252)`。改为毛玻璃：`var(--ata-glass-soft)` + `blur(14px)` + 10px 圆角 + 内描边；
实测浅色 `rgba(255,255,255,0.45)`、深色 `rgba(255,255,255,0.07)`，两套主题都不再出现白色实心块。
